# Verification Report: initprompt-extract-overflow

**Status**: PASS
**Branch**: feature/initprompt-extract-overflow
**Verified**: 2026-06-12
**Change Document**: [docs/changes/initprompt-extract-overflow.md](initprompt-extract-overflow.md)
**Test Protocol**: [docs/changes/tst-initprompt-extract-overflow.md](tst-initprompt-extract-overflow.md)

---

## Summary

Default agent-session init prompt template now contains one new bullet (verbatim, last item of the "Keep it minimal and action-oriented" list) instructing agents to offload topics that grow past ~5 bullets into a dedicated file beside `context.md`. Implementation updates all three `defaultInitPrompt` copies in `src/extension.ts` and the `package.json` setting `default` field consistently. All test cases pass.

---

## Test Results

| TC | Title | Method | Result |
|----|-------|--------|--------|
| TC-1 | Bullet present verbatim (session entity) | Unit test (`src/tests/initprompt-extract-overflow.test.ts`) | ✅ PASS |
| TC-2 | Bullet is last item of the discipline list | Unit test | ✅ PASS |
| TC-3 | Project entity receives the identical bullet | Unit test | ✅ PASS |
| TC-4 | Event entity receives the identical bullet | Unit test | ✅ PASS |
| TC-5 | Pre-existing five bullets unchanged in wording and order | Unit test | ✅ PASS |
| TC-6 | Custom-template override path unbroken | Unit test (structural) | ✅ PASS |
| T-7 (REQ_UAT_APT_INITPROMPT) / T-14 (SPEC) | Default init prompt contains the extract-overflow bullet verbatim as last list item | Unit test (TC-1 + TC-2 cover this) | ✅ PASS |

**Automated:** 14/14 new tests pass; 36/36 full suite pass. `npm run compile` clean.

**Manual UAT in Extension Development Host:** Not required for this CR — verification is fully covered by the deterministic unit tests on the prompt string content. There is no user-facing UX surface beyond the rendered prompt text that the unit tests already assert.

---

## Findings

### MECE F-1 (QM, rejected — out of scope, pre-existing) — DISPUTED

QM flagged inconsistent template-literal escaping across the three `defaultInitPrompt` copies in `src/extension.ts` as a FAIL-blocking finding.

- `git blame` confirmed the inconsistency originates in commit `28d2f7e0` (2026-05-23), three weeks before this CR. Only the two new bullet lines per copy belong to this CR.
- Functional analysis: both escaping styles produce identical rendered prompts; all 36 tests pass.
- CM session memory rule: "Repo housekeeping / chores without product impact are NOT in CM scope."
- **Disposition:** Disclosed to PM; not bundled into this CR. PM may open a separate refactor CR.

### MECE F-2 (QM, rejected — derivative) — DISPUTED

Derivative test-gap observation of F-1 (test does not assert literal `${kind}` placeholder syntax). Same out-of-scope rationale.

### Documentation gap (Docu Engineer, out of scope for this CR) — for PM queue

`docs/releasenotes.md` has no entry for this change. A one-liner should be added to the next-version section during the release process. This is a `syspilot.release` task, not feature-branch work.

### No regressions, no in-scope contradictions, no test coverage gaps.

---

## Verbatim Wording Drift Check

✅ PASS — the extract-overflow bullet appears character-for-character identical in:
- `src/extension.ts` (3 copies of `defaultInitPrompt`)
- `package.json` `jarvis.agentSession.initPromptTemplate` `default`
- `SPEC_EXP_AGENTSESSION_INITPROMPT` (`docs/design/spec_exp.rst`)
- `REQ_EXP_AGENTPROMPT_TEMPLATE` AC-7 (`docs/requirements/req_exp.rst`)
- `NEW_BULLET` constant in `src/tests/initprompt-extract-overflow.test.ts`

---

## Sign-off

- [x] All test cases executed
- [x] All in-scope findings resolved; out-of-scope findings disclosed
- [x] Acceptance criteria met
- [x] Verbatim wording preserved across all five locations
- [x] Ready for merge

---

*Generated by syspilot Change Manager*
