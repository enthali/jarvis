# Verification Report: listprojects-shape-parity

**Status**: PASS
**Branch**: feature/listprojects-shape-parity
**Verified**: 2026-06-12
**Change Document**: [docs/changes/listprojects-shape-parity.md](listprojects-shape-parity.md)
**Test Protocol**: [docs/changes/tst-listprojects-shape-parity.md](tst-listprojects-shape-parity.md)
**Closes**: F-18 (deferred from v0.7.0 entity-parity UAT)

---

## Summary

`jarvis_listProjects` now returns `{name, summary, agent, folder}`, matching the `jarvis_listSessions` shape. Both LM and MCP handler copies updated. All 7 test cases pass. No regressions in sibling tools.

---

## Test Results

| TC | Title | Method | Result |
|----|-------|--------|--------|
| TC-1 | All four fields returned; `summary`/`agent` populated from YAML | Unit test | ✅ PASS |
| TC-2 | Field order matches `jarvis_listSessions` (`{name, summary, agent, folder}`) | Unit test | ✅ PASS |
| TC-3 | Project with missing `agent` returns `agent: ""` | Unit test | ✅ PASS |
| TC-4 | Project with missing `summary` and `agent` returns `""` for both | Unit test | ✅ PASS |
| TC-5 | MCP variant returns same updated shape | Unit test (source inspection) | ✅ PASS |
| TC-6 | `jarvis_listSessions` shape unaffected | Unit test | ✅ PASS |
| TC-7 | `jarvis_listEvents` shape unaffected | Unit test | ✅ PASS |

**Automated:** 44/44 tests pass (8 new + 36 pre-existing). `npm run compile` clean.

**Manual UAT in Extension Development Host:** End-to-end tool invocation not auto-testable, but all implementation paths are exercised by unit tests against real `testdata/projects/` fixtures. No manual UAT blocker.

---

## Findings

### No in-scope findings.

**Artefakt-Removal Check:**
- **(a) Active code:** CLEAN — `{name, folder}` pattern absent in `src/extension.ts` for listProjects; new `summary` and `agent` fields confirmed in both LM and MCP mapping copies
- **(b) Active docs:** CLEAN — US/REQ/SPEC and `docs/releasenotes.md` all updated to reflect new shape
- **(c) Acceptable historic stranding:** `docs/changes/v0.4.0/list-projects.md` documents the old `{name, folder}` shape (v0.4.0 release artifact, immutable archive)

**Out-of-scope finding for PM queue (Docu Engineer):** `README.md` "Message Queue & Session Tools" section omits `#listProjects`, `#listEvents`, `#createProject`, and `#createEvent` entirely. Incomplete end-user tool reference added in v0.4.0/v0.7.0. Recommend a README refresh pass — not bundled here.

---

## Sign-off

- [x] All test cases executed
- [x] No in-scope findings
- [x] Acceptance criteria met
- [x] Both LM and MCP mapping copies verified identical
- [x] F-18 closed
- [x] Ready for merge

---

*Generated by syspilot Change Manager*
