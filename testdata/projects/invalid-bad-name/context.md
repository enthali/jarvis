# Context: invalid-bad-name

## Finding
- `project.yaml` is intentionally invalid: `name` is integer `12345` instead of a string (per REQ_UAT_INVALID_SAMPLES).

## Next
- No action needed — this is a static invalid test fixture.
